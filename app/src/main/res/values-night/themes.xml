<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidActivityLifecycle" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color (Yellow). -->
        <item name="colorPrimary">@color/button_color</item>
        <item name="colorPrimaryVariant">@color/close_button_color</item>
        <item name="colorOnPrimary">@color/text_color</item>
        <!-- Secondary brand color (Optional). -->
        <item name="colorSecondary">@color/button_color</item>
        <item name="colorSecondaryVariant">@color/button_color</item>
        <item name="colorOnSecondary">@color/close_button_color</item>
        <!-- Status bar color (Set to close button black). -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/close_button_color</item>
        <!-- Customize your theme here. -->
    </style>
</resources>
